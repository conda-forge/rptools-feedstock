{% set name = "rptools" %}
{% set version = "5.11.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/brsynth/rptools/archive/{{ version }}.tar.gz
  sha256: 7695b36fa3c061e4c83149c0099e943ffbb90b2c2f59861d7da9527b1b100444

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install -vv .

requirements:
  host:
    - python >3.6
    - pip
  run:
    - python >3.6
    - brs_utils
    - requests
    - urllib3
    - cobra
    - timeout-decorator
    - rdkit
    - python-libsbml
    - numpy
    - pandas
    - scipy
    - equilibrator-api
    - equilibrator-pathway
    - openbabel
    - colored
    - networkx >=2.5
    - filetype
    - rr_cache
    - rxn_rebuild
    - chemlite
    - h5py
    - xgboost
    - beautifulsoup4

test:
  source_files:
    - tests
  imports:
    - rptools
  requires:
    - pytest
    - pytest-cov
    - pytest-mock
  commands:
    - python -m rptools --help
    - cd tests
    - pytest -v --cov --cov-report term-missing

about:
  home: https://github.com/brsynth/rptools
  summary: Suite of tools that work on rpSBML data
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - breakthewall
    - kenza12
